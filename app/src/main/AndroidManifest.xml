<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ttl.operators_rxandroid">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity
            android:name=".operators.AsyncSubjectActivity"
            android:label="AsyncSubject" />
        <activity
            android:name=".operators.BehavoiurSubjectActivity"
            android:label="BehaviourSubject" />
        <activity
            android:name=".operators.BufferOperatorActivity"
            android:label="Buffer Operator"></activity>
        <activity
            android:name=".operators.FilterOperatorActivity"
            android:label="Filter Operator">

        </activity>
        <activity android:name=".operators.FlowableOperatorActivity"
            android:label="Flowable Task"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>